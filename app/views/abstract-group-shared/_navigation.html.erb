  <h4>Explore Groups</h4>
  <p<%= "#{' class="nav_highlight"' if current_page?(abstract_group.root_path) || current_page?(abstract_group.groups_path)}" %>>
    <%= link_to "Full list of Groups (#{Group.count})", abstract_group.groups_path %>
  </p>
  <h4>Manage Groups</h4>
  <p<%= "#{' class="nav_highlight"' if current_page?(abstract_group.new_group_path)}" %>>
    <%= link_to "Create New Group", abstract_group.new_group_path %>
  </p>